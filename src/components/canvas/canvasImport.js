// Separate file: canvasImports.js
import { Canvas, useFrame } from "@react-three/fiber";
import {
  Points,
  PointMaterial,
  Preload,
  useGLTF,
  Float,
  OrbitControls,
  useTexture,
} from "@react-three/drei";

export {
  Canvas,
  useFrame,
  Points,
  useGLTF,
  PointMaterial,
  Preload,
  Float,
  OrbitControls,
  useTexture,
};
